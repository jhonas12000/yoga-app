{
  "name": "fullstack-app",
  "version": "1.0.0",
  "scripts": {
    "start": "cd backend && node server.js",
    "heroku-postbuild": "cd frontend && npm install && npm run build"
  },
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "express": "^5.2.1",
    "mongoose": "^9.4.1",
    "cors": "^2.8.6",
    "dotenv": "^17.4.1"
  }
}